{
  "name": "bushfire-sentinel",
  "version": "1.0.0",
  "description": "Revolutionizing Australian Bushfire Management with Telstra's Network APIs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

